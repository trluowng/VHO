node_modules
dist
dist-ssr
backend/logs/
# The runtime SQLite DB (accounts, password hashes, health profiles) must never be
# committed. Everything else under backend/data/ (pricing/RAG reference data etc.)
# is legitimate tracked content, so only the DB files themselves are excluded.
backend/data/*.db
backend/data/*.db-journal
.env
.env.local
*.local
.DS_Store
