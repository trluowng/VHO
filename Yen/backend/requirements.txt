# Live model provider SDKs (install the one you use):
openai>=1.0.0
anthropic>=0.34.0
google-genai>=0.8.0
# Research tools:
requests>=2.31.0
PyYAML>=6.0
pypdf>=4.0.0
# Web server + auth (accounts, health profile, calendar, cycle tracking):
fastapi>=0.110
uvicorn>=0.29
PyJWT>=2.8
# Conversation memory: embeddings (bge-m3) + FAISS vector store
sentence-transformers>=2.3.0
faiss-cpu>=1.8.0
numpy>=1.24
# Browser-recorded Vietnamese speech-to-text (/stt/transcribe):
SpeechRecognition>=3.10
# Vietnamese text-to-speech (/tts) -- generates MP3 in-memory, no local playback
gTTS>=2.5.0
