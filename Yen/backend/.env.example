# Copy to .env and fill in — never commit .env (gitignored).

# --- Gemini (AI thật cho triage) ---
GEMINI_API_KEY=
# TRIAGE_MODEL=gemini-2.5-flash

# --- Groq (OpenAI-compatible, mặc định qwen/qwen3-32b) ---
# TRIAGE_PROVIDER=groq
GROQ_API_KEY=
# GROQ_BASE_URL=https://api.groq.com/openai/v1
# TRIAGE_MODEL=qwen/qwen3-32b

# --- fetch tool (tuỳ chọn, không cần cho demo triage cơ bản) ---
# FIRECRAWL_API_KEY=

# --- tài khoản / JWT (đổi sang chuỗi ngẫu nhiên thật khi deploy production) ---
# JWT_SECRET=doi-chuoi-nay-thanh-random-that-dai

# --- server ---
# PORT=8787
